FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    location    "0";
    object      U;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 1 -1 0 0 0 0];

Uref (1.649194 0 0);

internalField   uniform $Uref;

boundaryField
{
    AXIS
    {
        type            empty;
    }
    INLET
    {
        type            fixedValue;
        value           uniform $Uref;
    }
    OUTLET
    {
        type            inletOutlet;
        inletValue      uniform (0 0 0);
        value           uniform $Uref;
    }
    HULL
    {
        type            fixedValue;
        value           uniform (0 0 0);
    }
    FRONT
    {
        type            wedge;
    }
    BACK
    {
        type            wedge;
    }
}